name: Update GitHub activity

on:
  schedule:
    - cron: "30 1 * * *"
  workflow_dispatch:
    inputs:
      model:
        description: OpenAI model override (leave blank to use OPENAI_MODEL variable)
        required: false
        type: string

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Update activity data
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ inputs.model || vars.OPENAI_MODEL || 'gpt-5.4-mini' }}
        run: node scripts/update-github-activity.mjs
      - name: Commit changes
        run: |
          if git diff --quiet -- README.md; then
            echo "GitHub activity is already up to date."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "chore: update GitHub activity"
          git push
